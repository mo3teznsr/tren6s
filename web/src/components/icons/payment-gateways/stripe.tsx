import Image from "next/image";

export const StripeIcon = ({ ...props }) => {
  return (
    <Image src="https://tren6s.com/api/img/stripe.webp" alt="Stripe" {...props} width={120} height={120}  />
  );
};
